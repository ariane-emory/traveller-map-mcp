{
  "tools": [
    {
      "name": "get_universe",
      "description": "Get the list of all sectors in the Traveller universe",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "name": "get_sector",
      "description": "Get data for a specific sector in the Traveller universe",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          },
          "format": {
            "type": "string",
            "description": "Data format (sec, t5ss, json, etc.)",
            "default": "json"
          }
        },
        "required": ["sector"],
        "additionalProperties": false
      }
    },
    {
      "name": "traveller_map_search",
      "description": "Search for sectors, subsectors, worlds, or regions in the Traveller universe. Notes on query string format: All searches are case-insensitive. By default, only matches at the start of names or after spaces are returned (e.g., \"sol\" matches \"Sol\", \"Solomani Rim\", and \"Nowa Sol\" but not \"Marsol\"). Wildcard support: \"*\" and \"%\" - zero or more of any character; \"?\" and \"_\" - exactly one character; \"[]\" - character ranges (e.g., [0-5], [m-z], [89ABC]); Example: \"r*a\" matches \"Regina\". Word matching behavior: Specifying a wildcard within a word turns off \"start of word\" matching. Multiple space-delimited words are joined as logical AND clauses. Example: \"so ri\" matches \"Solomani Rim\". Special prefixes: \"exact:\" - force exact match (e.g., \"exact:sol\"); \"like:\" - \"sounds like\" match (e.g., \"like:tear\" finds \"Terra\"); \"uwp:\" - search UWP fields (e.g., \"uwp:a*\" finds class A starports); \"pbg:\" - match PBG fields; \"zone:\" - match Zone fields; \"alleg:\" - match Allegiance fields; \"stellar:\" - match stellar data (e.g., \"stellar:\\\"M? I*\\\"\" for red giants); \"remark:\" - require specific remarks (e.g., \"remark:Hi\" for high-population worlds); \"in:\" - search within a specific sector (e.g., \"t* in:spin\"). UWP Shortcut: Querying with XXXXXXX-X format (7 characters, hyphen, 1 character) is a shortcut for \"uwp:\". Search Scope Limitation: Cannot perform scoped searches like \"TL-F worlds in the Solomani Rim\". Searches are performed on individual items (sectors, subsectors, worlds).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query term"
          },
          "formatted": {
            "type": "boolean",
            "description": "Whether to format the JSON output with indentation",
            "default": true
          }
        },
        "required": ["query"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_route",
      "description": "Get a route between two locations in the Traveller universe",
      "inputSchema": {
        "type": "object",
        "properties": {
          "start": { 
            "type": "string",
            "description": "Starting location (sector/hex or world name)"
          },
          "end": {
            "type": "string",
            "description": "Ending location (sector/hex or world name)"
          },
          "jumps": {
            "type": "number",
            "description": "Number of jumps allowed",
            "default": 4
          }
        },
        "required": ["start", "end"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_sector_metadata",
      "description": "Get metadata for a specific sector in the Traveller universe",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          }
        },
        "required": ["sector"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_hex_data",
      "description": "Get data for a specific hex in a sector in the Traveller universe",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          },
          "hex": {
            "type": "string",
            "description": "Hex location (e.g., \"0101\")"
          }
        },
        "required": ["sector", "hex"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_sector_image",
      "description": "Get an image of a Traveller universe sector",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          },
          "style": {
            "type": "string",
            "description": "Image style (atlas, poster, print, candy, draft, fugue)",
            "default": "poster"
          },
          "width": {
            "type": "number",
            "description": "Width of the image in pixels"
          },
          "height": {
            "type": "number",
            "description": "Height of the image in pixels"
          }
        },
        "required": ["sector"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_subsector_image",
      "description": "Get an image of a Traveller universe subsector",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          },
          "subsector": {
            "type": "string",
            "description": "Subsector letter (A-P)"
          },
          "style": {
            "type": "string",
            "description": "Image style (atlas, poster, print, candy, draft, fugue)",
            "default": "poster"
          },
          "width": {
            "type": "number",
            "description": "Width of the image in pixels"
          },
          "height": {
            "type": "number",
            "description": "Height of the image in pixels"
          }
        },
        "required": ["sector", "subsector"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_worlds_in_jump_range",
      "description": "Get worlds in the Traveller universe within a jump distance from a specific hex",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": {
            "type": "string",
            "description": "Name of the sector"
          },
          "hex": {
            "type": "string",
            "description": "Hex location (e.g., \"0101\")"
          },
          "jump_distance": {
            "type": "number",
            "description": "Jump distance (0 through 12)",
            "minimum": 0,
            "maximum": 12
          }
        },
        "required": ["sector", "hex", "jump_distance"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_world_wiki_url",
      "description": "Construct a wiki URL for a Traveller world",
      "inputSchema": {
        "type": "object",
        "properties": {
          "world_name": {
            "type": "string",
            "description": "Name of the world"
          },
          "sector_name": {
            "type": "string",
            "description": "Name of the sector (optional)"
          },
          "hex": {
            "type": "string",
            "description": "Hex location (optional)"
          }
        },
        "required": ["world_name"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_subsector_wiki_url",
      "description": "Construct a wiki URL for a Traveller subsector",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subsector_name": {
            "type": "string",
            "description": "Name of the subsector"
          }
        },
        "required": ["subsector_name"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_sector_wiki_url",
      "description": "Construct a wiki URL for a Traveller sector",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector_name": {
            "type": "string",
            "description": "Name of the sector"
          }
        },
        "required": ["sector_name"],
        "additionalProperties": false
      }
    }
  ]
}
